import React from 'react';
import {StyleSheet, View} from 'react-native';

interface FaceSmileProps {}

function FaceSmile({}: FaceSmileProps) {
  return <View></View>;
}

const styles = StyleSheet.create({});

export default FaceSmile;
