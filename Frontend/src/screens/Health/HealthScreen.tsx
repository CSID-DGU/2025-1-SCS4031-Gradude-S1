import React from 'react';
import {Text, View} from 'react-native';

interface HealthScreenProps {}

function HealthScreen({}: HealthScreenProps) {
  return (
    <View>
      <Text>health화면</Text>
    </View>
  );
}

// const styles = StyleSheet.create({});

export default HealthScreen;
